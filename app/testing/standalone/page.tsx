"use client";

import MenuSelect from "@/components/MenuSelect";

export default function TestingPage() {
    return (
        <MenuSelect
            id=""
            title="this:key"
            value=""
            options={[{value:"",label:""}]}
            onChange={() => []}
        />
    )
}
